import Vue from 'Vue'
import Router from 'vue-router'

import BookView from '/src/views/BookView'


Vue.use(Router)

export default new Router({
  routes: [
    {
      path: '/',
      redirect: '/pages/'
    },
    {
      
    }
  ]
})